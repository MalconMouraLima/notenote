.wrapper_with_padding
  #notes.clearfix
    - unless @notes.blank?
      - @notes.each do |note|
        %a{href: (url_for [note])}
          .note
            %p.title= note.title
            %p= time_ago_in_words(note.created_at)
    - else
      %h2 Add a note
      %p It appers you haven't created any notes yet... Lets fix that. Why don't you create a new note below.
      %button= link_to "New note", new_note_path
